version: "3.2"

services:
  monit:
    build: .
    container_name: monit
    ports:
      - "80:2812"
    volumes:
      - ./monitrc:/etc/monitrc
      - ./telegramrc:/etc/telegramrc
    restart: unless-stopped

